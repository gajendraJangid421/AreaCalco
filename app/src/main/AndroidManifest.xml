<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.areacalco">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AreaCalco">
        <activity android:name=".MainActivityRandom6"></activity>
        <activity android:name=".MainActivityRandom5" />
        <activity android:name=".MainActivityRandom4" />
        <activity android:name=".MainActivityRandom" />
        <activity android:name=".MainActivityTrapezium" />
        <activity android:name=".MainActivityRectangle" />
        <activity android:name=".MainActivityTriangle" />
        <activity android:name=".MainActivitySquare" />
        <activity android:name=".MainActivity2" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>